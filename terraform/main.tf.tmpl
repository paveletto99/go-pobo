terraform {
	required_version = ">= 1.5.0"

	required_providers {
		google = {
			source  = "hashicorp/google"
			version = "~> 5.0"
		}
	}
}

provider "google" {
	project = var.project
	region  = var.region
}

// Add shared APIs, networking, database, cache, key management, and secrets here.